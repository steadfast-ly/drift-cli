# spec/

This directory holds the canonical server's `openapi.json`, pushed automatically
by the server's release workflow. It is licensed under the repository's
Apache-2.0 licence. Hand edits are overwritten on the next server release.
